services:
  juno-dev:
    build:
      context: .
      dockerfile: Dockerfile
    env_file:
      - development.env
    restart: unless-stopped
  juno-prod:
    build:
      context: .
      dockerfile: Dockerfile
    env_file:
      - production.env
    restart: unless-stopped